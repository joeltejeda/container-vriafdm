name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  Deploy:
    environment: production
    runs-on: ubuntu-latest
    env:
      AMBER_SECRET: ${{ secrets.AMBER_SECRET }}
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server."
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Deploy
        run: |
          set -x
          echo Installing amber secrets manager
          curl -L https://github.com/fpco/amber/releases/download/v0.1.3/amber-x86_64-unknown-linux-musl > amber
          chmod +x ./amber
          mv ./amber /usr/local/bin/amber
          mkdir -p ~/.ssh
          amber exec -- sh -c 'ssh-keyscan $DOKKU_HOST >> ~/.ssh/known_hosts'
          eval `ssh-agent -s`
          ssh-add - <<< "${{ secrets.DOKKU_SSH_PRIVATE_KEY}}"
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY dokku apps:create container-vriafdm || true'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY dokku builder:set container-vriafdm build-dir src'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY dokku builder-dockerfile:set container-vriafdm dockerfile-path Dockerfile'

          # Set common env settings
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_USER=$DB_USER; dokku apps:list"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_PASSWORD=$DB_PASSWORD"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_HOST=$DB_HOST"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_PORT=$DB_PORT"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_NAME=$DB_NAME"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm RAILS_DEVELOPMENT_HOSTS=container-vriafdm.containers.anotherwebservice.com"'
          # Database connection string (rails)
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DATABASE_URL=$RAILS_DATABASE_URL"'
          # Database connection string (django)
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm SECRET_KEY=$DJANGO_SECRET_KEY"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm ALLOWED_HOSTS=$DJANGO_ALLOWED_HOSTS"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DEBUG=$DJANGO_DEBUG"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_ENGINE=$DJANGO_ENGINE"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_NAME=$DJANGO_DB_NAME"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_HOST=$DJANGO_DB_HOST"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_USER=$DJANGO_DB_USER"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_PASSWORD=$DJANGO_DB_PASSWORD"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku config:set --no-restart container-vriafdm DB_PORT=$DJANGO_DB_PORT"'

          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY dokku git:sync --build container-vriafdm https://github.com/joeltejeda/container-vriafdm.git main'
          # Assign letsencrypt wildcard certificate
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C "$CONTAINER_HOSTING_API_KEY dokku certs:add container-vriafdm < cert-key.tar"'
          #testing
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dokku apps:list"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "ls"'
          amber exec -- sh -c 'ssh dokku@$DOKKU_HOST -C $CONTAINER_HOSTING_API_KEY "dig @DOKKU_HOST"'
          amber exec -- sh -c 'dig $DOKKU_HOST'
          amber exec -- sh -c 'echo $CONTAINER_HOSTING_API_KEY >> test.txt; cat test.txt'
          

